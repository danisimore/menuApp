version: "3.7"

services:
  db:
    image: postgres:15
    container_name: db_container
    env_file:
      - .env-prod
  app:
    build:
      context: .
    env_file:
      - .env-prod
    container_name: fastapi_app
    command: ["/fastapi_app/docker/app.sh"]
    ports:
      - "8000:8000"
    depends_on:
      - db
